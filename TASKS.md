# Задачи (текущая пачка)

Пишу сюда ДО выполнения, чтобы ничего не забыть. Ставлю [x] когда готово + коммит.

## Новая пачка (кэш + оценка в чате + ясность UX)
- [x] A. Кэш ИИ-объяснения на карточке. Word.explainCache + миграция; explainWord
      отдаёт кэш если есть, иначе генерит и сохраняет; updateWord сбрасывает кэш
      при смене смысловых полей. Проверено: #1 6.7с → #2 119мс; PATCH → NULL → регенер.
- [x] B. Free-recall в чате: «✍️ Проверь меня» (testMeSeed) в ExplainChat; промпт
      askAboutWord учит квизить и по-доброму оценивать. Проверено: тутор просит
      привести пример с словом.
- [x] C. Ясность UX: компонент BotInfo — блок «что умеет бот» на странице Аккаунта
      (EN/RU/ZH) + кнопка «Открыть @bot», заменил тонкую ссылку-секцию.

## Из последних сообщений
- [x] 1. Транскрипция при тапе (пиньинь/ромадзи/романизация) + настройка вкл/выкл.
      Готово. «Баг» был в шелле (curl ломал UTF-8); в браузере gloss корректен:
      醒来→просыпаться (xǐng lái), 海鸥→чайка (hǎi ōu).
- [x] 2. Тап-перевод на СОЧЕТАНИЯХ (collocations) на странице слова — компонент
      TapGlossPills (портал-поповер, кэш, api.gloss с withTranscription по настройке);
      заменил статичный Pills на странице слова.
- [x] 3. Ридер: переделать панель кнопок — компактный ряд чипов (Save/My texts/
      AI text/Scan/Sample/Clear), «Читать» — отдельная широкая primary-кнопка.
- [x] 4. Ридер: сохранять текст и ДО, и ПОСЛЕ перевода — кнопка 💾 в тулбаре чтения
      открывает ту же SaveModal.
- [x] 5. Коллекции для сохранённых текстов — поле collection у ReaderText,
      чипы-фильтры в «Мои тексты», бейдж коллекции у элемента.
- [x] 6. «Текст от ИИ» в ридере — спрашивать уровень языка (CEFR) перед генерацией.
      Готово: сегментированный выбор A1–C2 в модалке генерации, дефолт = getLevel(lang) ?? B1.
- [x] 7. Сохранение текста: SaveModal — заголовок (префилл) или «✨ пусть ИИ назовёт»
      (autoName → titleFor на бэке), поле коллекции с подсказками существующих.
      ИСТОЧНИК не трогал — он остался в форме ввода для быстрого добавления слов.
- [x] 8. Карточки в процессе создания (пустое значение) — показывать плейсхолдер/
      скелетон «значение создаётся» или анимацию, либо не показывать пустую строку.
      Готово: shimmer (animate-pulse) в ячейке значения, пока meaningZh пустой.

## План по 3/4/5/7 (ридер, делаю пачкой)
- Бэкенд: ReaderText.collection (String?) + миграция; listTexts фильтр по коллекции;
  createText принимает collection + autoName (ИИ придумывает заголовок); listCollections.
- API: saveReaderText(collection?, autoName?); readerTexts(collection?); readerCollections().
- SaveModal (task 7): поле заголовка (префилл) + «✨ пусть ИИ назовёт» + коллекция
  (input + подсказки существующих). Источник НЕ трогаю.
- ReaderTextTools (task 3): компактная панель — Save/My texts/AI text через SaveModal.
- LibraryModal (task 5): чипы-фильтры по коллекциям.
- Ридер (task 4): кнопка Save и в режиме чтения (та же SaveModal).

## Правила
- Перед задачей — записать сюда.
- После — [x] + git commit в new_lexa (без Co-Authored-By).
